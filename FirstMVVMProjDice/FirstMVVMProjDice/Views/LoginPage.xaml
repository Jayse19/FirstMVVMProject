<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="FirstMVVMProjDice.Views.LoginPage"
             Shell.NavBarIsVisible="False">
    <ContentPage.Content>
        <StackLayout Orientation="Vertical">
            <Label Text="Login" FontSize="Large" HorizontalOptions="Center"/>
            <Label Text="Enter your Username"/>
            <Entry Placeholder="Username" Text="{Binding Username}"/>
            <Label Text="Enter your Password" />
            <Entry Placeholder="Password"  Text="{Binding Password}"/>
            <Button Text="Login" Command="{Binding LoginCommand}" HorizontalOptions="FillAndExpand"></Button>
            <Label Text="{Binding LoginInfo}" />
        </StackLayout>

    </ContentPage.Content>
</ContentPage>